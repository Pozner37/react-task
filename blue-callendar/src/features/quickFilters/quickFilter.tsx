import React from 'react';
import {Button} from "@material-ui/core";

function QuickFilter(props: any) {
    const filter = props.filterName;
    return (
        <>
            <Button></Button>
        </>
    );
}

export default QuickFilter;